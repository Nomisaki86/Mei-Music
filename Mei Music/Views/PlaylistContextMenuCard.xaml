<UserControl x:Class="Mei_Music.PlaylistContextMenuCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Border Background="White" CornerRadius="10" Padding="4,6"
            MinWidth="170">
        <Border.Effect>
            <DropShadowEffect BlurRadius="18" ShadowDepth="3" Opacity="0.18" Color="#000000" Direction="270"/>
        </Border.Effect>

        <StackPanel>
            <!-- Delete Playlist option -->
            <Button x:Name="DeleteButton"
                    Click="DeleteButton_Click"
                    Background="Transparent"
                    BorderThickness="0"
                    Cursor="Hand"
                    HorizontalContentAlignment="Stretch">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BtnBorder" Background="Transparent"
                                CornerRadius="6" Padding="10,8">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <!-- Delete icon using opacity mask for theme tinting -->
                                <Rectangle Width="16" Height="16"
                                           Fill="#6B7380"
                                           RenderOptions.BitmapScalingMode="Fant"
                                           SnapsToDevicePixels="True"
                                           UseLayoutRounding="True"
                                           VerticalAlignment="Center">
                                    <Rectangle.OpacityMask>
                                        <ImageBrush ImageSource="/Resources/Images/delete.png"
                                                    Stretch="Uniform"
                                                    RenderOptions.BitmapScalingMode="Fant"/>
                                    </Rectangle.OpacityMask>
                                </Rectangle>
                                <TextBlock Text="Delete Playlist"
                                           FontSize="13.5"
                                           Foreground="#444444"
                                           VerticalAlignment="Center"
                                           Margin="10,0,0,0"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#F0F2F5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Border>
</UserControl>
